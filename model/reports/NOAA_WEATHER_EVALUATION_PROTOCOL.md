# Fixed evaluation of the original NOAA forecast replication

Written before retrieving or classifying the planned 2021–2023 weather fields. The request plan is `47747f5a0216b5b0a43e9df5777575bfafb165ad6616332869eda35c2b508258`. This protocol supplements its fixed stadium identities, dates, interpolation, forecast initialization and unchanged weather thresholds. It does not change the live weather policy or the four-candidate prospective evaluation.

First complete the inventory and field archives, retaining explicit source failures. Derive and save weather measurements and classifications before joining scores or computing returns. Do not substitute another initialization, venue, forecast product or threshold after encountering unavailable fields or unfavorable results. Require original object Last-Modified before the affected 06:30 Eastern decision, the planned forecast lead/publication buffer, valid GRIB identity and grid metadata, and finite physically valid measurements. A missing required field makes that game's weather unavailable.

Archive timestamps are **availability proxies**, not independently certified publication receipts. AWS specifies that Last-Modified for a multipart upload reflects upload initiation, which can precede completed public availability. Preserve the ETag and multipart indicator and retain this caveat in every result. The existing timestamp gate and initialization buffer remain unchanged; multipart storage alone does not exclude a game. This qualification was added before weather-field classification. [AWS metadata semantics](https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingMetadata.html).

The primary sample is every planned, weather-available game with a valid repaired reference total and final score. Use exactly the existing repaired market-source precedence. Match game IDs one-to-one and recheck season and teams. Final totals include overtime. The strategy takes Under only when the fixed three-condition intersection holds. Risk one hypothetical unit at assumed −110: win +100/110, loss −1, integer push 0. Include pushes in the staked denominator and report them separately. Pending or invalid outcomes remain explicitly unavailable. This cannot reconstruct actual morning offered prices or sportsbook execution.

Report the selected strategy's count, wins/losses/pushes, units and ROI for each season and pooled. Report taking every Under in the identical available sample as the fixed benchmark, and disclose the nonselected games separately. Show results by repaired market source without selecting a favorable source. The primary contrast is selected-strategy ROI minus the all-Under benchmark on the common coverage. Report the exact coverage funnel and every exclusion reason.

Use whole Monday–Sunday Eastern kickoff-calendar weeks for 10,000 bootstrap draws, seed 20260909. Resample the same week draws for the strategy and benchmark. Include zero-selection weeks in the covered-week bootstrap, omit draws with no selected stakes, and report their count. Show descriptive 95% and 99% intervals for strategy ROI and its paired benchmark contrast. Also report active-week cluster-ratio-score t intervals and leave-one-week-out ROI ranges to expose concentration. Do not treat any one interval as a confirmatory profitability test: prior outcomes have been reused, cross-week dependence may remain, and the full earlier research search is unknown.

Keep this NOAA replication separate from the 2024–2025 Open-Meteo study and the 2026 archived-price replay. Differences in forecast grid, initialization convention, interpolation, era and market source prevent an unqualified pooled performance claim. Compare their direction and magnitude transparently. A favorable result would strengthen the case for the already registered future weather experiment; an unfavorable or empty result must be published equally. Neither outcome by itself establishes executable positive EV or authorizes real wagers.
